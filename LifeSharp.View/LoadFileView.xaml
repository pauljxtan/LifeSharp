<UserControl x:Class="LifeSharp.View.LoadFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LifeSharp.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:ViewModel="clr-namespace:LifeSharp.ViewModel;assembly=LifeSharp.ViewModel">

    <UserControl.DataContext>
        <ViewModel:LoadFileViewModel></ViewModel:LoadFileViewModel>
    </UserControl.DataContext>

    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="5">File path:</TextBlock>
            <TextBlock Text="{Binding FilePath}" Margin="5"></TextBlock>
        </StackPanel>
        <Button Command="{Binding ShowDialogCommand}" Margin="5">Load from file</Button>
    </StackPanel>
</UserControl>
